<?xml version="1.0" encoding="UTF-8"?>
<odoo>
<!-- 
    <!-- Action for Claims -->
    <record id="action_claim_list" model="ir.actions.act_window">
        <field name="name">Claims</field>
        <field name="res_model">claim.claim</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create a new claim to track customer issues and complaints.</p>
        </field>
    </record>

    <!-- Tree View for Claims -->
    <record id="view_claim_tree" model="ir.ui.view">
        <field name="name">claim.claim.tree</field>
        <field name="model">claim.claim</field>
        <field name="arch" type="xml">
            <tree string="Claims">
                <field name="name" string="Reference"/>
                <field name="date" string="Date"/>
                <field name="subject" string="Subject"/>
                <field name="claimant_id" string="Claimant"/>
                <field name="agency_id" string="Agency"/>
                <field name="state" string="Status"/>
                <field name="priority" string="Priority"/>
                <field name="type" string="Type"/>
                <field name="urgent" string="Urgent"/>
            </tree>
        </field>
    </record>

    <!-- Form View for Claims -->
    <record id="view_claim_form" model="ir.ui.view">
        <field name="name">claim.claim.form</field>
        <field name="model">claim.claim</field>
        <field name="arch" type="xml">
            <form string="Claim">
                <sheet>
                    <group>
                        <field name="name" string="Reference" readonly="1"/>
                        <field name="date" string="Date"/>
                        <field name="subject" string="Subject"/>
                        <field name="description" string="Description"/>
                        <field name="claimant_id" string="Claimant"/>
                        <field name="agency_id" string="Agency"/>
                        <field name="project_id" string="Related Project"/>
                        <field name="team_id" string="Assigned Team"/>
                        <field name="origin" string="Origin"/>
                        <field name="source" string="Source"/>
                        <field name="type" string="Type"/>
                        <field name="urgent" string="Urgent"/>
                        <field name="complexity" string="Complexity"/>
                        <field name="gravity" string="Gravity"/>
                        <field name="state" string="Status"/>
                        <field name="customer_service_agent_id" string="Customer Service Agent"/>
                        <field name="attachment_ids" string="Attachments"/>
                    </group>

                    <!-- Commercial PV Section -->
                    <group string="Commercial PV">
                        <field name="commercial_pv" string="PV"/>
                        <field name="commercial_decision" string="Decision"/>
                        <field name="commercial_pv_date" string="PV Date"/>
                    </group>

                    <!-- Customer Satisfaction Section -->
                    <group string="Customer Satisfaction">
                        <field name="satisfaction_score" string="Satisfaction Score"/>
                        <field name="satisfaction_comment" string="Satisfaction Comment"/>
                    </group>
                </sheet>

                <!-- Buttons for State Transitions -->
                <header>
                    <button name="action_submit" string="Submit" type="object" class="btn-primary" states="draft"/>
                    <button name="action_start_processing" string="Start Processing" type="object" class="btn-primary" states="submitted"/>
                    <button name="action_resolve" string="Resolve" type="object" class="btn-primary" states="in_progress"/>
                    <button name="action_close" string="Close" type="object" class="btn-primary" states="resolved"/>
                    <button name="action_archive" string="Archive" type="object" class="btn-secondary" states="closed"/>
                </header>
            </form>
        </field>
    </record>

    <!-- Search View for Claims -->
    <record id="view_claim_search" model="ir.ui.view">
        <field name="name">claim.claim.search</field>
        <field name="model">claim.claim</field>
        <field name="arch" type="xml">
            <search string="Claim Search">
                <field name="name" string="Reference"/>
                <field name="subject" string="Subject"/>
                <field name="claimant_id" string="Claimant"/>
                <field name="agency_id" string="Agency"/>
                <field name="state" string="Status"/>
                <field name="type" string="Type"/>
                <field name="urgent" string="Urgent"/>
                <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]"/>
                <filter string="In Progress" name="in_progress" domain="[('state', '=', 'in_progress')]"/>
                <filter string="Resolved" name="resolved" domain="[('state', '=', 'resolved')]"/>
                <filter string="Closed" name="closed" domain="[('state', '=', 'closed')]"/>
                <group expand="1" string="Group By">
                    <filter string="Status" name="group_by_state" context="{'group_by': 'state'}"/>
                    <filter string="Type" name="group_by_type" context="{'group_by': 'type'}"/>
                    <filter string="Agency" name="group_by_agency" context="{'group_by': 'agency_id'}"/>
                </group>
            </search>
        </field>
    </record> -->
</odoo>