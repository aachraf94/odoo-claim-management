<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- Action for Employees -->
    <record id="action_employee_list" model="ir.actions.act_window">
        <field name="name">Employees</field>
        <field name="res_model">hr.employee</field>
        <field name="view_mode">tree,form,search</field>
        <field name="domain">[('agency_id', '!=', False)]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Manage employees involved in claim management.
            </p>
        </field>
    </record>

    <!-- Inherit Tree View for Employees -->
    <record id="view_employee_tree" model="ir.ui.view">
        <field name="name">hr.employee.tree.inherit</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_tree"/>
        <field name="arch" type="xml">
            <tree position="inside">
                <field name="agency_id"/>
                <field name="role"/>
            </tree>
        </field>
    </record>

    <!-- Inherit Form View for Employees -->
    <record id="view_employee_form" model="ir.ui.view">
        <field name="name">hr.employee.form.inherit</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Claim Management" name="claim_management">
                    <group>
                        <group>
                            <field name="agency_id"/>
                            <field name="role"/>
                        </group>
                    </group>
                </page>
            </notebook>
        </field>
    </record>

    <!-- Search View for Employees -->
    <record id="view_employee_search" model="ir.ui.view">
        <field name="name">hr.employee.search.inherit</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_filter"/>
        <field name="arch" type="xml">
            <search position="inside">
                <field name="agency_id"/>
                <field name="role"/>
                <filter string="Customer Service" name="customer_service" domain="[('role', '=', 'customer_service')]"/>
                <filter string="Technician" name="technician" domain="[('role', '=', 'technician')]"/>
                <filter string="Commercial" name="commercial" domain="[('role', '=', 'commercial')]"/>
                <group expand="0" string="Group By">
                    <filter string="Agency" name="group_by_agency" domain="[]" context="{'group_by': 'agency_id'}"/>
                    <filter string="Role" name="group_by_role" domain="[]" context="{'group_by': 'role'}"/>
                </group>
            </search>
        </field>
    </record>


    <menuitem id="menu_employees" 
        name="Employees" 
        parent="menu_operations" 
        action="action_employee_list"
        sequence="10"/>

</odoo>